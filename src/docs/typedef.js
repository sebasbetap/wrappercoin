/**
 * It represents an axios error
 * @typedef {Object} ErrorAxios
 * @property {String} values see axios documentation
 */

/**
 * It represents an axios error
 * @typedef {Object} ResponseAxios
 * @property {String} values see axios documentation
 */

/**
 * cryptocurrency structure in the database
 * @typedef {Object} CryptocurrencyModel
 * @property {string} id primary key
 * @property {string} name cryptocurrency name
 * @property {userId} age user's foreign key
 */

/**
 * It represents a User
 * @typedef {Object} ResponseCryptocurrency
 * @property {string} name cryptocurrency name
 * @property {string} price document id of user
 * @property {string} source age of user
 */
